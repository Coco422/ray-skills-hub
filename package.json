{
  "name": "ray-skills-hub-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev --host 127.0.0.1",
    "build": "astro build",
    "preview": "astro preview --host 127.0.0.1",
    "validate": "python3 scripts/validate_catalog.py"
  },
  "dependencies": {
    "astro": "^6.4.6",
    "gray-matter": "^4.0.3",
    "marked": "^15.0.12",
    "yaml": "^2.8.0"
  },
  "overrides": {
    "esbuild": "^0.28.1"
  }
}
